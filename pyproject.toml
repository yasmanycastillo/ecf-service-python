[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ecfservice"
version = "0.4.0"
description = "SDK Python oficial para emite.do — e-CF ante la DGII"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.10"
authors = [{ name = "ECF Service SRL" }]
keywords = ["ecf", "dgii", "facturacion-electronica", "dominican-republic"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Office/Business :: Financial",
]
dependencies = [
    "httpx>=0.27",
    "pydantic>=2.0",
    "python-stdnum>=2.0",
]

[project.urls]
Documentation = "https://docs.emite.do/sdks/python"
Homepage = "https://docs.emite.do"
Repository = "https://github.com/yasmanycastillo/ecf-service-python"

[project.optional-dependencies]
dev = ["pytest>=8.0", "pytest-asyncio>=0.23", "respx>=0.21"]

[tool.hatch.build.targets.wheel]
packages = ["ecfservice"]
